# Ralph Progress Log
Started: Fri Apr 10 20:28:09 CST 2026
---

## Codebase Patterns
- Chrome extension project using Vite + React + TypeScript
- State management via React Context (src/store/index.tsx)
- No test framework configured; quality checks = `npx tsc --noEmit`
- date-fns used for date formatting with locale support (zhCN, enUS, ja)
- `initReactI18next` plugin auto-binds i18n to React — no I18nextProvider wrapper needed when using default instance
- `noUnusedLocals` and `noUnusedParameters` are enabled in tsconfig — be careful not to import unused things
- Background service worker (src/background/index.ts) cannot import React — use `i18next` directly with inline locale imports, NOT `../i18n` (which pulls in react-i18next)
- Non-React utility files (date.ts, report.ts) can import `i18n` from `../i18n` since they run in the React context
- Template i18n uses `{{t:key.path}}` syntax to distinguish from data placeholders `{{key}}` in weekly report template
- `getDateFnsLocale()` exported from date.ts — reuse in report.ts for locale-aware date formatting
- `PRIORITY_CONFIG.label` is an i18n key string (e.g. 'task.priorityHigh'), not a translated string — components must call `t()` on it

## [2026-04-10] - US-001
- Installed i18next and react-i18next
- Created src/i18n/index.ts with i18n initialization (navigator.language detection, fallback to zh)
- Created locale files: src/i18n/locales/zh.json, en.json, ja.json with ALL hardcoded strings from the entire codebase
- Added `import '../i18n'` in src/newtab/main.tsx to initialize before React renders
- Exported `useTranslation` from src/i18n/index.ts for convenience
- Typecheck passes
- Files changed: src/i18n/index.ts (new), src/i18n/locales/zh.json (new), src/i18n/locales/en.json (new), src/i18n/locales/ja.json (new), src/newtab/main.tsx (modified), package.json, package-lock.json
- **Learnings for future iterations:**
  - The locale JSON files contain ALL strings from the entire codebase, organized by component/area (nav, greeting, task, worklog, pomodoro, weekly, report, settings, date, tag, notification, encouragements)
  - Japanese encouragement messages use natural Japanese phrasing, not just translations
  - The `encouragements` array is a top-level key since it's used as an array in components
  - `useTranslation` is re-exported from `src/i18n/index.ts` so components can import from a single module
---

## [2026-04-10] - US-002
- Removed ENCOURAGEMENTS array from types/index.ts (already in locale files)
- Changed PRIORITY_CONFIG labels to i18n key strings ('task.priorityHigh', 'task.priorityMedium', 'task.priorityLow')
- Changed DEFAULT_SETTINGS.weeklyReport.template to use `{{t:key}}` i18n placeholders
- Updated src/utils/date.ts: all functions (formatDate, formatRelativeDate, getGreeting, formatDuration) now use i18n.t() for strings and dynamic date-fns locale
- Updated src/utils/report.ts: all Chinese template strings replaced with i18n.t() calls, locale-aware date formatting
- Updated src/background/index.ts: standalone i18n setup (no React), notification strings use i18n.t()
- Updated PomodoroTimer.tsx: switched from ENCOURAGEMENTS constant to locale-based encouragements via useTranslation
- Added report.dayTotal key to all 3 locale files for "(共 Xh)" pattern
- Exported getDateFnsLocale() from date.ts for reuse in report.ts
- storage.ts had no Chinese strings — no changes needed
- Files changed: src/types/index.ts, src/utils/date.ts, src/utils/report.ts, src/background/index.ts, src/components/pomodoro/PomodoroTimer.tsx, src/i18n/locales/zh.json, en.json, ja.json
- **Learnings for future iterations:**
  - Background service worker must NOT import from `../i18n` — it pulls in react-i18next/React. Use bare `i18next` with inline locale JSON imports
  - The `{{t:key.path}}` convention distinguishes i18n placeholders from data placeholders in the weekly report template
  - PRIORITY_CONFIG.label is now an i18n key — any future component using .label must call t() on it
  - t('encouragements', { returnObjects: true }) returns the full array from locale files
---

## [2026-04-10] - US-003
- Header.tsx: added useTranslation() hook to subscribe to language changes (greeting text already i18n'd via getGreeting() in US-002)
- BottomBar.tsx: replaced hardcoded Chinese nav labels with i18n keys (nav.home, nav.worklog, nav.weekly, nav.settings), added useTranslation hook
- NewTab.tsx: no text strings to internationalize (only a loading spinner with no text) — no changes needed
- Files changed: src/components/layout/Header.tsx, src/components/layout/BottomBar.tsx
- **Learnings for future iterations:**
  - Calling useTranslation() without destructuring is valid and won't trigger noUnusedLocals — it subscribes the component to language changes as a side effect
  - NewTab.tsx has no user-facing text, only a loading spinner — no i18n needed there
  - navItems array pattern: change `label: string` to `labelKey: string` and call `t(labelKey)` in render for i18n
---

## [2026-04-10] - US-004
- Internationalized all task component hardcoded Chinese strings
- TaskList.tsx: replaced "今日待办", "添加", "已完成", empty state, modal titles, toast messages with i18n keys
- TaskForm.tsx: replaced placeholders ("任务名称...", "描述（可选）"), priority label, priority button text (using PRIORITY_CONFIG[p].label + t()), duration/date labels, cancel/save/add buttons
- TaskFilter.tsx: replaced status option labels with i18n keys (changed `label` to `labelKey` pattern), tag expand/collapse text
- Tag.tsx: added useTranslation, changed TAG_COLORS keys from Chinese strings to i18n keys (e.g. '工作' → 'tag.work'), added displayLabel resolution for i18n tag keys
- TagSelector.tsx: added useTranslation, replaced "标签：" with i18n key
- types/index.ts: changed DEFAULT_SETTINGS.customTags from Chinese strings to i18n keys (['tag.work', 'tag.dev', ...])
- Added `task.tags` locale key to all 3 locale files for TagSelector label
- Files changed: src/components/task/TaskList.tsx, TaskForm.tsx, TaskFilter.tsx, src/components/ui/Tag.tsx, TagSelector.tsx, src/types/index.ts, src/i18n/locales/zh.json, en.json, ja.json
- **Learnings for future iterations:**
  - Default tags are now stored as i18n keys ('tag.work', 'tag.dev', etc.) — Tag component resolves them via `t(label, label)` with fallback to raw label for user-created tags
  - TAG_COLORS uses i18n keys as map keys now, NOT display text — color lookup still uses the raw key
  - `t(label, label)` second arg is defaultValue — if i18n key has no match, displays the key itself (useful for user-created tags that don't start with 'tag.')
  - TaskItem.tsx had no hardcoded text — formatDuration and formatRelativeDate are already i18n'd from US-002
---

## [2026-04-10] - US-005
- Replaced all 8 remaining hardcoded Chinese strings in PomodoroTimer.tsx with i18n t() calls
- Strings replaced: title/breakTitle, selectTask placeholder, emptyState, startFocus/startBreak, resume/pause, focusDone/breakDone toast messages, focusComplete prefix in work log content
- All locale keys (pomodoro.*) and encouragements were already in locale files from US-001
- No locale file changes needed — only PomodoroTimer.tsx was modified
- Files changed: src/components/pomodoro/PomodoroTimer.tsx
- **Learnings for future iterations:**
  - PomodoroTimer already had useTranslation and encouragement loading from US-002, only UI label strings needed replacing
  - `pomodoro.focusComplete` is used as a prefix concatenated with task title — works because locale value ends with full-width colon (：)
  - showToast messages can accept i18n t() calls directly since they expect strings
---

## [2026-04-10] - US-006
- Internationalized all work log component hardcoded Chinese strings
- WorkLogList.tsx: replaced "工作记录", "今日", "记录", "筛选：", "清除", empty state text, modal title, toast messages with i18n t() calls
- WorkLogForm.tsx: replaced "记录一下你做了什么...", "时长(分钟)", "日期", "取消", "记录" with i18n t() calls
- CalendarView.tsx: replaced hardcoded `zhCN` locale with dynamic `getDateFnsLocale()`, changed `'M月d日'` format to locale-aware `'MMM d'` for date range and weekday display
- All worklog locale keys were already defined in zh.json/en.json/ja.json from US-001 — no locale file changes needed
- Files changed: src/components/worklog/WorkLogList.tsx, WorkLogForm.tsx, CalendarView.tsx
- **Learnings for future iterations:**
  - `format(day, 'MMM d', { locale: getDateFnsLocale() })` produces "4月6日" for zh/ja and "Apr 6" for en — a single format string works across all three locales
  - CalendarView didn't need useTranslation hook since it has no text labels — only date formatting needed locale awareness
  - WorkLogForm labelKey pattern: Input `label` prop accepts t() calls directly since it expects a ReactNode
---

## [2026-04-10] - US-007
- Internationalized all WeeklyReport.tsx hardcoded Chinese strings
- Replaced: title, currentWeek label, stats labels (workLogs, completedTasks, totalHours), description text, generate/regenerate/copy buttons, toast messages, settings modal title/labels/day-of-week options/template label/cancel/save buttons
- Replaced hardcoded `zhCN` locale with dynamic `getDateFnsLocale()` for week date range formatting
- Added `common.cancel` and `common.save` locale keys to all 3 locale files (reusable across components)
- Added `weekly.templateHint` locale key for the placeholder hint text
- Report template default content was already locale-aware from US-002 (uses `{{t:key}}` syntax)
- Generated report text was already i18n'd in US-002 (report.ts uses i18n.t())
- Files changed: src/components/weekly/WeeklyReport.tsx, src/i18n/locales/zh.json, en.json, ja.json
- **Learnings for future iterations:**
  - `common.cancel` and `common.save` added as shared locale keys — future components should reuse these instead of component-specific cancel/save keys
  - WeeklyReport had a lot of duplicated Monday-calculation logic (3 inline filters) — this is pre-existing tech debt, not introduced by i18n
  - The `weekly.day.0` through `weekly.day.6` keys were already defined from US-001 — always check existing locale keys before adding new ones
---

## [2026-04-10] - US-008
- Internationalized all SettingsPage.tsx hardcoded Chinese strings with i18n t() calls
- Replaced: page title, pomodoro section, input labels (focus/break/long break/interval), tags section, tag display names (resolved via `tag.startsWith('tag.') ? t(tag, tag) : tag`), add tag placeholder/button, data management section, data notice, clear data confirm dialog/button, save settings button, toast message
- Modal.tsx: no text strings (close button is X icon only) — no changes needed
- Toast.tsx: no default text (messages passed via props) — no changes needed
- Tag.tsx and TagSelector.tsx: already internationalized in US-004 — no changes needed
- All locale keys already existed in zh.json/en.json/ja.json from US-001 — no locale file changes needed
- Files changed: src/components/settings/SettingsPage.tsx
- **Learnings for future iterations:**
  - Tag display in SettingsPage uses same `tag.startsWith('tag.') ? t(tag, tag) : tag` pattern as Tag.tsx — consider extracting to a shared utility if it appears again
  - Modal.tsx has no user-facing text — the close button is only an X icon, not text
  - SettingsPage tag list renders raw spans (not Tag component) — needed inline i18n resolution rather than using Tag component
---

## [2026-04-10] - US-009
- Added language switcher UI at the top of SettingsPage with three buttons: 简体中文, English, 日本語
- Exported `changeLanguage()` from src/i18n/index.ts that calls `i18n.changeLanguage()` and persists to `chrome.storage.local` under key `yuzutask_language`
- On init, `loadSavedLanguage()` checks `chrome.storage.local` for saved preference first, falls back to `navigator.language` detection
- Language change takes effect immediately via i18next React binding (no page reload needed)
- date-fns locale switches accordingly because `getDateFnsLocale()` reads `i18n.language` dynamically
- Added `settings.languageSection` and `settings.language` locale keys to all 3 locale files
- Manual browser verification needed (no browser testing tools available)
- Files changed: src/i18n/index.ts, src/components/settings/SettingsPage.tsx, src/i18n/locales/zh.json, en.json, ja.json
- **Learnings for future iterations:**
  - i18next `changeLanguage()` triggers React re-renders automatically when using `useTranslation()` hook — no manual forceUpdate needed
  - `getDateFnsLocale()` in date.ts already reads `i18n.language` dynamically, so date-fns locale switches automatically when language changes
  - The `i18n` instance from `useTranslation()` can be used to read `i18n.language` for comparing current language
  - Background service worker still uses its own standalone i18n setup and won't pick up the language preference — this is acceptable since the background only sends notifications
---
